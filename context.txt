Act as a Lead C Security Engineer. We are fixing a legacy "Hotel Reservation System" in C on Windows (MinGW).

## Project State: Critical Audit Findings
I have performed a code audit and found 6 specific issues you must help me fix. Keep these in your context:

1.  **SECURITY (Critical):** `reservations.c` uses `scanf` for string input. It allows buffer overflows. WE MUST REPLACE THIS with `fgets` or the project's `safe_input` module.
2.  **DATA CORRUPTION (Critical):** `fichiers.c` ignores `fread` return values. It risks loading garbage data.
3.  **OS CONFLICT (Critical):** `fichiers.c` uses Linux syscalls. We are on Windows. We must switch to standard C or Windows API.
4.  **LOGIC (High):** ID generation in `clients.c` and `reservations.c` is flawed (ID reuse).
5.  **LOGIC (High):** `calculer_nuits` in `reservations.c` calculates billing incorrectly.
6.  **INCONSISTENCY:** The `safe_input` module exists but is ignored in favor of `scanf`.

## Your Mission
I will paste the code for each file one by one. You will:
1.  Identify the specific lines containing the bugs listed above.
2.  Rewrite the code to fix the issue.
3.  Ensure the fix works on **Windows**.

I am ready to start with Issue #1.